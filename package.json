{
  "name": "wedding-rsvp-seating-system-netlify",
  "version": "3.0.0",
  "private": true,
  "description": "Wedding Guest, RSVP & Seating Management System — Netlify Functions + Supabase backend, deployable as-is.",
  "scripts": {
    "build": "node scripts/generate-config.js",
    "dev": "netlify dev",
    "seed": "node scripts/seed-demo.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "@supabase/supabase-js": "^2.45.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "netlify-cli": "^17.0.0"
  }
}
